# Maps the Upgrade request header to the correct Connection header value.
# Required for WebSocket proxying — must live at http{} scope (conf.d/ is included there).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
